<br>
<h1>管理者 ユーザー編集画面</h1>

<%= form_with(model: [:admin, @user], local: true ) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.email_field :email %>
      <%= f.check_box :admin %>
  <%= f.submit '送信する'%>

<% end %>
